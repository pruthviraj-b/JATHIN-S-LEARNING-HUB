# Frontend (Next.js)

Quick start (PowerShell):

```powershell
cd frontend
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:4000` by default. Adjust calls in `pages/index.js` if needed.
