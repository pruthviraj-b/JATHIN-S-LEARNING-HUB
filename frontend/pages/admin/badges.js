export default function AdminBadges() {
    return (
        <div style={{ padding: 50, color: 'red', background: 'white', minHeight: '100vh', zIndex: 9999, position: 'relative' }}>
            <h1>EXTREME DEBUG MODE</h1>
            <p>NO LAYOUT. JUST TEXT.</p>
            <p>If you see this, the page itself loads fine.</p>
        </div>
    );
}
